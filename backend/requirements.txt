# Core numerical & data wrangling
numpy>=2.1
pandas>=2.2
scipy>=1.12
scikit-learn>=1.5
matplotlib>=3.8
tqdm>=4.66
python-dotenv>=1.0

# Reinforcement learning stack (A2C, PPO, etc.)
stable-baselines3>=2.2.1
cloudpickle>=3.0

# Portfolio optimization helpers
cvxpy>=1.4.3
scs>=3.2.4

# Finance & technical analysis utilities
pandas_datareader>=0.10
ta>=0.11.0
yfinance>=0.2.37

# FastAPI + ASGI server
fastapi>=0.111
uvicorn[standard]>=0.30

# Forecasting stack (ARIMA / LSTM / Autoformer)
statsmodels>=0.14.5           # NumPy-2-compatible ARIMA
einops>=0.7                   # needed by transformer implementations

# PyTorch (CPU version for production deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
